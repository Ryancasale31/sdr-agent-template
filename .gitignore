.env
.env.*
*.env
__pycache__/
*.pyc
_reimport.py
# radar_finds.json
scored_companies.json
# pipeline.json and icp_summary.json are committed as the deployed app's
# baseline data (Streamlit Cloud wipes runtime files on reboot). Repo is
# public and this is public business data — do NOT add contact PII here.
# sequences.json / hubspot_import.csv / tiga_import.csv stay ignored (contacts).
sequences.json
hubspot_import.csv
tiga_import.csv
pipeline_export.csv

# Screenshots and scratch output from Claude sessions
Claude outputs/
# Superseded entry point, kept locally only (see app.py)
app_v2.py
# Attendee / contact exports — PII, never in a public repo
*.csv
